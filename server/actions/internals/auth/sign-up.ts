export async function signUp(params: {
  user: {
    fullName: string;
    email: string;
    password: string;
  };
  token: string;
}) {
  // hash password
  // check if user already exists
  // create user
}
